version: '3.8'

services:
  it-asset-management:
    build: .
    container_name: it-asset-demo
    ports:
      - "8080:8080"
    restart: unless-stopped
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
